<h1><%= "#{t('.title')} (#{@products.count})" %></h1>

<%= link_to t('.new_btn'), new_product_path if authenticated? %>

<hr />

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
